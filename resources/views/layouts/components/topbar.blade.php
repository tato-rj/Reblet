    <div id="topbar" class="text-white position-relative py-3 d-none d-md-block" style="background: rgba(0,0,0,0.3); z-index: 1; font-size: 14px;">
      <div class="container">
        <div class="d-flex justify-content-end text-white">
          <a href="tel:{{config('brand.phone')}}" class="mr-4">@fa(['icon' => 'phone-alt']) {{config('brand.phone')}}</a>
          <a href="tel:{{config('brand.emails.info')}}">@fa(['icon' => 'envelope']) {{config('brand.emails.info')}}</a>
        </div>
      </div>
    </div>